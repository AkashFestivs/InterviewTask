<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Image Form Extraction</title>
</head>
<body>
  <h1>Upload Form Image</h1>
  <form action="/upload-new" method="post" enctype="multipart/form-data">
    <input type="file" name="image" accept="image/*" required />
    <button type="submit">Upload</button>
  </form>

  <% if (extractedData) { %>
    <h2>Extracted Data</h2>
    <form>
      <label>Name:</label>
      <input type="text" value="<%= extractedData.name %>" disabled /><br />
      
      <label>Mobile:</label>
      <input type="text" value="<%= extractedData.mobile %>" disabled /><br />
      
      <label>Date of Birth:</label>
      <input type="text" value="<%= extractedData.dob %>" disabled /><br />
      
      <label>Address:</label>
      <input type="text" value="<%= extractedData.address %>" disabled /><br />
    </form>
  <% } %>
</body>
</html>
